// test/contacts.test.ts
// This test file is deprecated since we removed the HighLevelApiClient and ContactsMCP classes
// The functionality is now handled by direct fetch calls in the main index.ts

describe("Contacts API (Deprecated)", () => {
  it("should be replaced with direct MCP calls", () => {
    // This test is a placeholder since the original classes were removed
    expect(true).toBe(true);
  });
});
